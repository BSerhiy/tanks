using UnityEngine;
using System.Collections;

public class CameraManager : MonoBehaviour
{
    [SerializeField] private Camera cameraTank, cameraShell, cameraGeneral;
    [SerializeField] private Transform camTankPivot, camShellPivot, camGeneralPivot;

	// Use this for initialization
	void Start ()
    {
	
	}
	
	// Update is called once per frame
	void Update ()
    {
	
	}
}
